{{#each errors}}
    <div class="alert alert-danger card w-50">
        {{{text}}}
    </div>
{{/each}}

<div class="card w-50">
    <div class="card-header">
        <h3>Acessar a área restrita</h3>
    </div>
    <div class="card-body">
        <form action="/user/validateUser" method="post">
            <label for="email">E-mail</label>
            <input class="form-control" type="text" name="email" id="email" placeholder="Email" required>
            <label for="password">Senha</label>
            <input class="form-control" type="password" name="password" id="password" placeholder="Senha" required>
            <br>
            <button type="submit" class="btn btn-success mt-2">Login</button>
            <a href="/" id="cancel" name="cancel" class="btn btn-secondary mt-2">Cancelar</a>
        </form>
    </div>
</div>    